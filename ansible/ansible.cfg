[defaults]
inventory = inventory.ini
roles_path = roles
stdout_callback = default
result_format = yaml
nocows = True
interpreter_python = auto_silent
retry_files_enabled = False

[privilege_escalation]
# become is requested per-task (system package installs only); never globally.
become = False
